-- Local / CI seed after migrations. Keep minimal; campaign data belongs in migrations or ops tools.
-- Core tables pre-exist on the linked project; repo migrations are additive deltas only.
select 1;
